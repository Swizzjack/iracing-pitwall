# ts-rs 10 joins `export_to` with TS_RS_EXPORT_DIR (default: "bindings/").
# Setting it to "." makes `export_to = "../shared/"` resolve to
# /work/shared/ instead of /work/bridge/shared/.
[env]
TS_RS_EXPORT_DIR = "."
